Start each reply with "Bn coae coae. Asa facem."
End each reply by running the command `git status`. Never forget to run this command as it is useful for tracking changes.
The environment is Windows and the shell is PowerShell, so use commands compatible with pwsh, not linux commands.
Never edit files unless I say so.
Whenever the user says "Lookin' crispy, that drip's crazy", you must stage the changes (add), then commit and then push to master on the remote to the master branch.
